{
  "name": "northbound-commerce",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test tests/booking.spec.js --workers=1",
    "check": "node --check js/main.js && node --check api/save-lead.js && node --check api/reddit-capi.js",
    "build": "npm run check"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "jsdom": "^29.1.1"
  }
}
